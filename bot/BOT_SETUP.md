# Быстрый запуск Telegram бота

## Шаг 1: Создание бота в Telegram

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и создайте бота
4. Сохраните токен бота (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 3: Настройка .env файла

1. Создайте файл `.env` в корне проекта (там же, где `bot.py`)
2. Добавьте в него следующие строки:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
GEMINI_API_KEY=ваш_gemini_api_ключ
OPENROUTER_API_KEY=ваш_openrouter_api_ключ
```

**Важно:**
- Не используйте кавычки вокруг значений
- Не добавляйте пробелы вокруг знака `=`
- Файл `.env` должен быть в той же директории, что и `bot.py`
- `GEMINI_API_KEY` - обязателен (основной провайдер)
- `OPENROUTER_API_KEY` - опционален (резервный провайдер для автоматического переключения)

**Пример содержимого `.env`:**
```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
GEMINI_API_KEY=AIzaSyAbCdEfGhIjKlMnOpQrStUvWxYz1234567
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_MODEL=openai/gpt-4o-mini
```

**Настройка модели OpenRouter (опционально):**
Вы можете изменить модель OpenRouter, добавив в `.env` файл:
```env
OPENROUTER_MODEL=название_модели
```

**Популярные модели OpenRouter:**
- `openai/gpt-4o-mini` - быстрая и дешевая (по умолчанию)
- `openai/gpt-4o` - более мощная версия
- `openai/gpt-4-turbo` - очень мощная модель
- `anthropic/claude-3.5-sonnet` - отличное качество
- `anthropic/claude-3-haiku` - быстрая модель
- `google/gemini-pro-1.5` - альтернатива Gemini
- `meta-llama/llama-3.1-70b-instruct` - открытая модель
- `mistralai/mistral-large` - качественная модель

Полный список доступных моделей: https://openrouter.ai/models

**Примечание:** Файл `.env` не должен попадать в систему контроля версий (git). Убедитесь, что он добавлен в `.gitignore`.

### 🔄 Автоматическое переключение между AI провайдерами

Бот автоматически переключается на OpenRouter при ошибках Gemini (503, 500, 429):
- **Основной провайдер:** Gemini (используется по умолчанию)
- **Резервный провайдер:** OpenRouter (активируется автоматически при ошибках)

**Как получить API ключи:**
- **Gemini:** https://aistudio.google.com/app/apikey (бесплатно)
- **OpenRouter:** https://openrouter.ai/keys (требуется регистрация)

**Рекомендация:** Установите оба ключа для максимальной надежности. Если Gemini недоступен, бот автоматически переключится на OpenRouter.

## Шаг 4: Проверка наличия шаблонов

Убедитесь, что в корне проекта есть файлы:
- `example.json` - JSON шаблон
- `example_cv_docx.docx` - Word шаблон

## Шаг 5: Запуск бота

```bash
python bot.py
```

Вы должны увидеть сообщение: `Бот запущен и готов к работе!`

## Шаг 6: Использование

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Отправьте PDF файл с резюме
4. Дождитесь обработки (1-2 минуты)
5. Получите готовый DOCX файл

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/status` - Показать информацию о настройках AI моделей (какая модель используется, какие ключи установлены)

## Решение проблем

### Ошибка "TELEGRAM_BOT_TOKEN не найден в .env файле"
- Убедитесь, что файл `.env` создан в корне проекта (там же, где `bot.py`)
- Проверьте, что в файле есть строка `TELEGRAM_BOT_TOKEN=ваш_токен`
- Убедитесь, что нет пробелов вокруг знака `=`
- Проверьте, что токен указан без кавычек

### Ошибка "Шаблон не найден"
- Убедитесь, что файлы `example.json` и `example_cv_docx.docx` находятся в той же директории, что и `bot.py`

### Ошибка при обработке файла
- Убедитесь, что установлен `GEMINI_API_KEY` (обязательно) или `OPENROUTER_API_KEY` (опционально)
- При ошибке 503 от Gemini бот автоматически переключится на OpenRouter (если ключ установлен)
- Проверьте, что PDF файл содержит текст (не только изображения)
- Убедитесь, что PDF файл не поврежден

### Ошибка 503 от Gemini API
Если вы видите ошибку 503 от Gemini:
- Бот автоматически попытается переключиться на OpenRouter (если `OPENROUTER_API_KEY` установлен)
- Если OpenRouter ключ не установлен, вы увидите сообщение об ошибке
- **Решение:** Добавьте `OPENROUTER_API_KEY` в `.env` файл для автоматического резервирования

