# PDF to Markdown Converter & Processor

Набор скриптов для работы с PDF, Markdown и Word документами:
- **bot.py** - Telegram бот для преобразования PDF резюме в Word документ
- **pdf_to_md.py** - извлечение текста из PDF файлов и сохранение в Markdown
- **md_to_json.py** - преобразование Markdown резюме в JSON формат по шаблону example.json
- **json_to_docx.py** - заполнение Word документа данными из JSON с сохранением форматирования
- **docx_to_template.py** - преобразование обычного Word документа в шаблон с плейсхолдерами
- **pdf_to_docx.py** - мастер-скрипт для автоматического преобразования PDF -> MD -> JSON -> DOCX

## Установка

### Создание виртуального окружения (рекомендуется)

1. Создайте виртуальное окружение:
```bash
python -m venv venv
```

2. Активируйте виртуальное окружение:

   **В Windows PowerShell:**
   ```powershell
   venv\Scripts\Activate.ps1
   ```

   **В Windows CMD:**
   ```cmd
   venv\Scripts\activate.bat
   ```

   **В Linux/Mac:**
   ```bash
   source venv/bin/activate
   ```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

### Установка без виртуального окружения

Или установите напрямую:
```bash
pip install pdfplumber python-docx google-genai aiogram python-dotenv requests
```

### Получение API ключа Gemini

1. Перейдите в Google AI Studio: https://aistudio.google.com/app/apikey
2. Создайте бесплатный API ключ (пробный тариф)
3. Установите ключ одним из способов:
   - Создайте файл `.env` в корне проекта: `GEMINI_API_KEY=your_key` (рекомендуется для бота)
   - Переменная окружения: `set GEMINI_API_KEY=your_key` (Windows) или `export GEMINI_API_KEY=your_key` (Linux/Mac)
   - Или введите ключ при запуске скрипта

## Использование

### 🤖 Telegram бот (bot.py) - Рекомендуется для удобства

**Создание бота:**
1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Создайте файл `.env` в корне проекта и добавьте:
   ```env
   TELEGRAM_BOT_TOKEN=your_bot_token
   GEMINI_API_KEY=your_gemini_api_key
   ```

**Запуск бота:**
```bash
python bot.py
```

**Использование:**
1. Найдите вашего бота в Telegram
<details>
  <summary>тестовый бот</summary>1.1 Либо же можете воспользоваться[тестовым ботом https://t.me/Qoehfhsj_bot)
</details>
2. Отправьте команду `/start` для начала работы
3. Отправьте PDF файл с резюме
4. Дождитесь обработки (1-2 минуты)
5. Получите готовый DOCX файл

**Особенности:**
- ✅ Простой интерфейс через Telegram
- ✅ Автоматическая обработка PDF файлов
- ✅ Показывает прогресс обработки
- ✅ Обработка ошибок с понятными сообщениями
- ✅ Автоматическая очистка временных файлов

**Требования:**
- Токен Telegram бота (получить у @BotFather)
- GEMINI_API_KEY (для работы AI)
- Все шаблоны должны быть в корне проекта (example.json, example_cv_docx.docx)

### 0. Автоматическое преобразование PDF -> DOCX (pdf_to_docx.py) ⚡

**Базовое использование (рекомендуется):**
```bash
python pdf_to_docx.py resume.pdf
```
Автоматически выполнит всю цепочку: PDF -> MD -> JSON -> DOCX

**С указанием выходного файла:**
```bash
python pdf_to_docx.py resume.pdf --output result.docx
```

**С указанием модели:**
```bash
python pdf_to_docx.py resume.pdf --model gemini-2.5-flash
```

**С сохранением промежуточных файлов:**
```bash
python pdf_to_docx.py resume.pdf --keep-intermediate
```

**Пропуск отдельных шагов:**
```bash
# Пропустить шаг 1 (использовать существующий MD)
python pdf_to_docx.py resume.pdf --skip-step1

# Пропустить шаг 2 (использовать существующий JSON)
python pdf_to_docx.py resume.pdf --skip-step2

# Пропустить шаг 3 (только до JSON)
python pdf_to_docx.py resume.pdf --skip-step3
```

**Все опции:**
```bash
python pdf_to_docx.py resume.pdf --output result.docx --model gemini-2.5-pro --keep-intermediate
```

**Особенности:**
- ✅ Автоматически выполняет всю цепочку преобразований
- ✅ Показывает прогресс каждого шага
- ✅ Может пропускать отдельные шаги для повторной обработки
- ✅ Автоматически удаляет промежуточные файлы (или сохраняет с --keep-intermediate)
- ✅ Обрабатывает ошибки и показывает понятные сообщения

### 1. Извлечение текста из PDF (pdf_to_md.py)

**Базовое использование:**
```bash
python pdf_to_md.py document.pdf
```
Создаст файл `document.md` в той же директории.

**Указание выходного файла:**
```bash
python pdf_to_md.py document.pdf output.md
```

### 2. Преобразование Markdown в JSON (md_to_json.py)

**Базовое использование:**
```bash
python md_to_json.py document.md
```
Использует шаблон `example.json` и создает файл `document.json`.

**С указанием шаблона:**
```bash
python md_to_json.py document.md --template example.json
```

**С указанием выходного файла:**
```bash
python md_to_json.py document.md --output result.json
```

**С указанием модели:**
```bash
python md_to_json.py document.md --model gemini-2.5-flash
```

**Все опции:**
```bash
python md_to_json.py document.md --template example.json --output result.json --model gemini-2.5-pro
```

**Примеры моделей:**
- `gemini-2.5-flash` — быстрый и экономичный (рекомендуется)
- `gemini-2.5-pro` — более точный, но медленнее
- `gemini-1.5-pro-exp` — экспериментальная версия с расширенными возможностями

Полный список моделей: https://ai.google.dev/gemini-api/docs/models

**Особенности:**
- ✅ Использует структуру из `example.json` как шаблон
- ✅ Извлекает данные из MD резюме в структурированный JSON
- ✅ Сохраняет все исходные данные без изменений
- ✅ Не добавляет новую информацию (только извлекает из текста)
- ✅ Автоматически заполняет все поля структуры
- ✅ По умолчанию использует gemini-2.5-flash (можно указать модель вручную через `--model`)

### 3. Заполнение Word документа из JSON (json_to_docx.py)

**Базовое использование:**
```bash
python json_to_docx.py document.json
```
Использует шаблон `example_cv_docx.docx` и создает файл `document_filled.docx`.

**С указанием шаблона:**
```bash
python json_to_docx.py document.json --template example_cv_docx.docx
```

**С указанием выходного файла:**
```bash
python json_to_docx.py document.json --output result.docx
```

**Все опции:**
```bash
python json_to_docx.py document.json --template template.docx --output result.docx
```

**Плейсхолдеры в шаблоне Word:**
- Простые поля: `{{vacancy}}`, `{{pitch}}`, `{{foreign_language}}`, и т.д.
- Списки: `{{#skills_and_tools}}...{{/skills_and_tools}}`
- Блоки опыта работы: `{{#work_experience}}...{{/work_experience}}`
- Блоки проектов: `{{#project_experience}}...{{/project_experience}}`

**Особенности:**
- ✅ Сохраняет все форматирование исходного документа
- ✅ Автоматически клонирует блоки для множественных записей (опыт работы, проекты)
- ✅ Работает без нейронных сетей (локальная обработка)
- ✅ Поддерживает списки, таблицы и сложные структуры
- ✅ Клонирует стили параграфов и форматирование текста

## Требования

- Python 3.6+
- pdfplumber (для pdf_to_md.py)
- python-docx (для json_to_docx.py)
- google-genai (для md_to_json.py)
- Gemini API ключ (для md_to_json.py) - https://aistudio.google.com/app/apikey
- example.json (для md_to_json.py) - шаблон структуры JSON
- example_cv_docx.docx (для json_to_docx.py) - шаблон Word документа

## Примечания

### pdf_to_md.py
- Скрипт сохраняет текст со всех страниц PDF файла
- Между страницами добавляется двойной перенос строки
- Если PDF содержит только изображения, текст может быть пустым
- Защищенные от копирования PDF могут не обрабатываться корректно

### md_to_json.py
- Требует API ключ Gemini и файл шаблона `example.json`
- Извлекает данные из MD резюме и заполняет JSON структуру
- По умолчанию использует модель gemini-2.5-flash, можно указать любую другую
- Очень низкая температура модели (0.2) для точности извлечения данных
- Промпт настроен на извлечение ТОЛЬКО данных из текста, без добавления новой информации
- Автоматически объединяет извлеченные данные с шаблоном для сохранения структуры
- Если информация отсутствует в тексте, оставляет поля пустыми

### json_to_docx.py
- Требует файл шаблона Word (`example_cv_docx.docx`) и JSON файл с данными
- Работает полностью локально, без использования нейронных сетей
- Сохраняет все форматирование исходного документа (шрифты, стили, отступы)
- Автоматически клонирует блоки для множественных записей (опыт работы, проекты)
- Поддерживает плейсхолдеры для простых полей и блоков с маркерами начала/конца
- Клонирует стили параграфов и форматирование текста при создании новых блоков
- Автоматически определяет режим работы: с плейсхолдерами или по заголовкам

### docx_to_template.py
- Преобразует обычный Word документ в шаблон с плейсхолдерами
- Автоматически находит секции по ключевым словам (заголовкам)
- Добавляет плейсхолдеры для всех полей из структуры JSON
- Работает с параграфами и таблицами
- Сохраняет форматирование исходного документа
- Создает шаблон, готовый для использования с json_to_docx.py

**С указанием выходного файла:**
```bash
python docx_to_template.py document.docx --output template.docx
```

**Особенности:**
- ✅ Автоматически находит секции по ключевым словам
- ✅ Добавляет плейсхолдеры для простых полей: `{{vacancy}}`, `{{pitch}}`, и т.д.
- ✅ Добавляет маркеры для списков: `{{#skills_and_tools}}...{{/skills_and_tools}}`
- ✅ Добавляет маркеры для блоков: `{{#work_experience}}...{{/work_experience}}`
- ✅ Работает с таблицами и параграфами
- ✅ Сохраняет форматирование исходного документа

### pdf_to_docx.py
- Мастер-скрипт, объединяющий все три этапа преобразования
- Автоматически вызывает pdf_to_md.py, md_to_json.py и json_to_docx.py последовательно
- Показывает детальный прогресс каждого шага
- Поддерживает пропуск отдельных шагов для оптимизации повторных запусков
- Автоматически управляет промежуточными файлами (удаляет или сохраняет)
- Обрабатывает ошибки на каждом этапе с понятными сообщениями
